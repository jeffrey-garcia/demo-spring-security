# Configure the default spring active profile to pcf
spring.profiles.active=pcf

# Configure the app name in the earliest phase of service configuration
# Used by Eureka client registration 
spring.application.name=demo-spring-security